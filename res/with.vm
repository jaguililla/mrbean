/** @see #$field.Name */
public $clazz.Name $fieldName ($field.Type.CanonicalText $field.Name) {
    return new $clazz.Name (
    #foreach ($field in $fields)
        $field.Name#if ($foreach.hasNext), #end
    #end
    );
}
